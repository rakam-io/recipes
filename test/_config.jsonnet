{
  version: 1.1,
  name: 'demo recipe',
  variables: {
    sessionDurationInMinutes: {
      label: 'Session duration',
      type: 'numeric',
      default: 30,
      description: 'The session duration of the user events',
    },
    addToBasket: {
      type: 'table-select',
      default: 30,
      description: 'The session duration of the user events',
    }
  }
}
